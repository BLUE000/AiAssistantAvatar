# 意思決定ログ (2026-06-19 19:06:46)

## ウィンドウ全体のタブ化と下部タブバー配置
* **背景・理由**:
  * ユーザーのフィードバックに基づき、タブ切り替え機能がウィンドウ左ペインだけでなく画面全体にかかるように構成を刷新し、タブ切り替えバーを画面の最下部（South）に配置するため。
  * 「チャット」タブ選択時はアバター・入力UI・AI応答吹き出しをすべて含めた従来のレイアウトを保持し、「設定」タブ選択時はアバター等の表示領域を隠して設定入力フォームにウィンドウ全体（750px幅）を使用できるようにすることで、視認性および設定編集時の操作性を大幅に向上させるため。
* **変更ファイル**:
  * [MODIFY] [avatar_window.h](file:///d:/prog/C++/AiAssistantAvatar/src/ui/avatar_window.h) & [avatar_window.cpp](file:///d:/prog/C++/AiAssistantAvatar/src/ui/avatar_window.cpp):
    * 不要になった `m_leftPanel` の定義および生成コードを削除。
    * `QMainWindow` の `centralWidget` の直下レイアウトに `m_tabWidget` を配置し、ウィンドウ全体がタブで切り替わる構造に変更。
    * `m_tabWidget->setTabPosition(QTabWidget::South)` を設定し、タブバーを画面最下部（South）に配置。
    * 「チャット」タブ（`m_chatTab`）を左右2ペイン構造とし、左側に `m_avatarLabel` や入力・送信等のコントロール群を配置した `chatLeftPanel` を、右側に吹き出し装飾（`m_rightPanel`）をレイアウトする構成に変更。
    * 「設定」タブ（`m_settingsTab`）には、以前構築した動的設定編集フォームのみをレイアウトし、タブ切り替え時にアバターや吹き出しが非表示になるように変更。

## 意思決定ログの整理と管理フォルダの作成
* **背景・理由**:
  * 意思決定ログファイルが肥大化するのを防ぎ、更新履歴をクリーンかつ時系列に管理しやすくするため、プロジェクトルートにあった単一の `DecisionLog.md` を廃止し、`DecisionLog` フォルダ配下にタイムスタンプ付きの独立したマークダウンファイルとして随時追加・保存する形式に移行するため。
* **変更ファイル**:
  * [NEW] `d:/prog/C++/AiAssistantAvatar/DecisionLog` (ディレクトリ): 意思決定ログを格納する専用フォルダ。
  * [MODIFY] [.gitignore](file:///d:/prog/C++/AiAssistantAvatar/.gitignore): 新設した `DecisionLog` フォルダが Git 管理対象外になるように追記。
