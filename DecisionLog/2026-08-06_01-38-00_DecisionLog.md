# 決定ログ (Decision Log) - 2026-08-06 01:38:00

## 変更の概要 (Summary)
1. **「1回使うたびにレートリミット到達中になる」根本原因の完全解修復**:
   - `recordLocalConsumption`（1回使用時の減算処理）に 60 秒後の全量再補充タイマー（`nextResetAt`）自動起動を追加。
   - `updateAvailable` において、`s.nextResetAt.isValid()` だけの条件で無条件に `available = false` に判定されていた重大なロジック反転バグを解消し、`rpmRemaining > 0`（残枠あり）の期間は `available = true`（🟢 利用可能）を確実に維持するよう修正。
   - `rpmRemaining == 0` の場合のみ `available = false`（🔴 レートリミット到達中）に切り替わり、1分経過後に全量全枠自動復帰する完全動作を実現。
2. **フルクリーンからの公式認可リリース ＆ ZIP 納品**:
   `build` ディレクトリの削除・クリーンアップ後、`TRUSTCHAIN_BUILD_IS_CUSTOMIZED = 0` の公式認定ビルドを実行。`windeployqt` デプロイを経て [dist/AiAssistantAvatar_Release.zip](file:///d:/prog/C++/AiAssistantAvatar/dist/AiAssistantAvatar_Release.zip) (35.6 MB) へ最新化納品。

## 理由 (Why)
- **ユーザー様からの不具合報告およびご指示**:
  - 「Mistralが一回使うたびにレートリミットに達するんだけど。。。 なんならHuggingFaceもOpenRouter、SAKURAも。。。 念のためログはtmpに格納した」

## 成果物
- Verified Remote Commit: `d21141c` (`origin/master`)
- Zip Package: [AiAssistantAvatar_Release.zip](file:///d:/prog/C++/AiAssistantAvatar/dist/AiAssistantAvatar_Release.zip) (35.6 MB)
