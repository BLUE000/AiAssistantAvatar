# 意思決定記録: ファイル探索ロジックのヘルパー部品化 (resolveExistingFilePath)

## 実施日
2026-08-01 (19:26 JST)

## 理由・目的
- `loadSettingsToUI()` 内で約30行にわたって重複ベタ書きされていたファイルパス探索処理（`Config/local_settings.json` や `../local_settings.json` など）を汎用ヘルパー関数へ部品化・共通化し、コードの可読性・メンテナンス性を大幅に向上させるため。
- コンパイルスイッチ (`#ifdef PROJECT_SOURCE_DIR`) の位置および開発環境・実行環境の優先探索順序を厳密に保持して安全性を確保するため。

## 改修内容
- `src/ui/avatar_window.cpp`: 匿名ネームスペース内へヘルパー関数 `resolveExistingFilePath(const QString &fileName)` を導入し、`loadSettingsToUI()` のパス探索をわずか3行に集約・簡略化。

## テスト結果
- CTest (Google Test): `AiAssistantAvatarTest` 全 100% PASS 成功。
