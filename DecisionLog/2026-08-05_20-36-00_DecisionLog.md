# 決定ログ (Decision Log) - 2026-08-05 20:36:00

## 変更の概要 (Summary)
ユーザー様のご提案を取り入れた「UI表示トリガー要求 ＆ ルーター側オンデマンド評価型（タイマーフリー）レートリミット自動枠復帰 ＆ UTCタイムゾーン修正」の C++ ソースコード実装および単体テスト (`RateLimitTrackerTest` 2 件) の検証完了。

## 理由 (Why)
- **バグ原因の完全解消 ＆ 提案の完全適用**:
  - `RateLimitTracker::updateAvailable` / `isAvailable` / `statusOf` / `allStatuses` にて、現在 UTC 時刻 `currentDateTimeUtc()` での期限切れ判定（`nextResetAt <= nowUtc`）および使用枠自動再補充（`rpmRemaining = rpmMax`）、`available = true` 自律復帰ロジックを実装。
  - `RateLimitTabWidget::showEvent` にてタブ表示時に `requestRefreshStatus()` シグナルを発火し、マネージャー（`AIClientManager::emitCurrentStatus`）へ即時更新要求を送出・同期描画させるフローを接続。
  - `RateLimitTabWidget::updateProviderCard` でのカウントダウン計算を `QDateTime::currentDateTimeUtc()` に統一し、タイムゾーンのずれを根本解消。
  - UI 側のタイマーは 0 秒（完全非発火・タイマーフリー）とし、詰まりやクラッシュの可能性を 0% に遮断。
- **検証結果**:
  - Google Test (`RateLimitTrackerTest.AutoResetOnExpiredResetTime`, `RateLimitTrackerTest.UtcTimezoneCountdownCalculation`) が 100% 成功 (PASSED) することを確認。

## 実装ファイル
- [rate_limit_tracker.h](file:///d:/prog/C++/AiAssistantAvatar/src/ai/rate_limit_tracker.h)
- [rate_limit_tracker.cpp](file:///d:/prog/C++/AiAssistantAvatar/src/ai/rate_limit_tracker.cpp)
- [rate_limit_tab_widget.h](file:///d:/prog/C++/AiAssistantAvatar/src/ui/rate_limit_tab_widget.h)
- [rate_limit_tab_widget.cpp](file:///d:/prog/C++/AiAssistantAvatar/src/ui/rate_limit_tab_widget.cpp)
- [avatar_window.cpp](file:///d:/prog/C++/AiAssistantAvatar/src/ui/avatar_window.cpp)
- [test_ai_client.cpp](file:///d:/prog/C++/AiAssistantAvatar/test/test_ai_client.cpp)
