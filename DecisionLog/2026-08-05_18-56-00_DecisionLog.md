# 決定ログ (Decision Log) - 2026-08-05 18:56:00

## 変更の概要 (Summary)
TrustChain オンライン検証通信（`TrustChainCore.cpp`）における POST ペイロードのパラメータ不整合修正、Git Push、ならびに公式認定（ウォーターマーク消去）動作に対応したリリースビルド ＆ `dist/AiAssistantAvatar_Release.zip` 再パッケージング完了。

## 理由 (Why)
- **オンライン検証失敗の根本原因と修正**:
  - `TrustChainCore.cpp` 内で検証サーバー（`https://streamers-tool.sakura.ne.jp/TransCipher/index.php`）に送信していた JSON ペイロードに `"key"` および `"data"` パラメータが不足していたため、サーバー側が `{"status":"error","message":"Missing required parameters..."}` を返し、オンライン検証が失敗（`AuthStatus::Watermarked`）してタイトルバーおよびステータスバーにウォーターマーク `(Custom Build: ...)` が表示されていた。
  - `TrustChainCore.cpp` 内で `"key": m_apiToken` および `"data": ""` を埋め込むよう修復したことで、サーバー応答が `{"status":"success","token_status":"active",...}` となり、**ウォーターマーク表示のない公式認定ビルド（`AuthStatus::Normal`）へと正常昇格するよう修正**を完了した。
- **実施手順（TrustChain 規則順守）**:
  1. `Lib/TrustChain` サブモジュールおよびメインリポジトリで Commit ＆ Push (`16149fa` -> `origin/master`)。
  2. `git status` が `working tree clean` の状態でクリーン・リリースビルド生成。
  3. `windeployqt` 部署および [dist/AiAssistantAvatar_Release.zip](file:///d:/prog/C++/AiAssistantAvatar/dist/AiAssistantAvatar_Release.zip) (46.1 MB) へ圧縮・格納完了。

## 成果物
- Submodule Commit: `8e811cc`
- Main Repo Commit: `16149fa` (`origin/master`)
- Zip Package: [AiAssistantAvatar_Release.zip](file:///d:/prog/C++/AiAssistantAvatar/dist/AiAssistantAvatar_Release.zip) (46.1 MB)
